chef.controller 'TabListController', ($scope, $location) ->
  # $scope.tab = ''
  # $scope.$watch 'tab', -> console.log arguments
  # $scope.$on '$locationChangeSuccess', () ->
  #   if matched = $location.url().match /^\/([\w]*)\//
  #     # $scope.tab = matched[1]
  #     $scope.$evalAsync 'tab = ' + matched[1]